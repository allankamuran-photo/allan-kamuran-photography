import type { Metadata } from 'next';
import {categories,categoryPhotos} from '@/lib/gallery';
export const metadata:Metadata={title:'Work — Allan Kamuran'};
export default function Work(){return <main id="main-content" className="info-page work-index"><section className="info-heading"><p className="eyebrow">FOUR WAYS OF SEEING</p><h1>My <em>work.</em></h1><p className="info-lead">Weddings, passing moments, the natural world, and the people in it.</p></section><div className="collection-grid">{categories.map((category,i)=>{const photos=categoryPhotos(category.slug);return <a className="collection-card" href={'/work/'+category.slug} key={category.slug}>{photos[0]&&<img src={photos[0].src} alt={photos[0].alt} loading={i>1?'lazy':'eager'}/>}<div><span className="eyebrow">0{i+1} / {photos.length} PHOTOGRAPHS</span><h2>{category.name}<span>↗</span></h2><p>{category.description}</p></div></a>})}</div></main>}
